# BBOX Output

Wenn `execute_processing.sh --view z/lat/lon` mit dem `--view` Parameter ausgeführt wird, werden die output Daten in diesem Ordner erstellt.

Dies ist hilfreich, wenn nicht gesamt Berlin ausgeführt werden soll. 